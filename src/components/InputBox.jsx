import React from 'react'

const InputBox = ({
   lable,
   onAmountChange,
   onCurrencyChange,
   currencyOption = [],
   selectedCurrency = `USD`
}) => {
  return (
    <div>InputBox</div>
  )
}

export default InputBox